#include <iostream>
using namespace std;

#include <opencv2/core/core.hpp>
#include <opencv2/opencv.hpp>

void ComputeORB(cv::Mat image, vector<cv::KeyPoint> keypoints, cv::Mat& descriptor, int count = 32) {
    
}

void Match(cv::Mat descriptors_1, cv::Mat descriptors_2, vector<cv::DMatch> matches) {

}